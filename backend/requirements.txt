fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
transformers>=4.40.0
# torch installed separately in Dockerfile (CPU-only to save space)
# For local dev: pip install torch
numpy
requests
